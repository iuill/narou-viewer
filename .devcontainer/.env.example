# 複数 worktree の Dev Container を並行起動するための任意ポート設定。
# 既定ポート以外を使う worktree で、このファイルを `.devcontainer/.env` にコピーする。
# 既定ポートを使う worktree では `.devcontainer/.env` は不要。

VIEWER_WEB_HOST_PORT=5174
VIEWER_API_HOST_PORT=8081
VIEWER_WEB_E2E_HOST_PORT=15174
VIEWER_API_E2E_HOST_PORT=18081
